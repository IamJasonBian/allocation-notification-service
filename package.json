{
  "name": "allocation-notification-service",
  "version": "1.0.0",
  "description": "Job discovery and notification service for quant trading firms. Polls Greenhouse boards, diffs against Redis, sends Slack alerts.",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "netlify dev",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@netlify/blobs": "^10.6.0",
    "@netlify/functions": "^3.0.0",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.4.0"
  }
}
