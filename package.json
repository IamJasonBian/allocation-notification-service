{
  "name": "quant-job-tracker",
  "version": "1.0.0",
  "description": "Scrapes Greenhouse job boards for quant firms, diffs against Redis, sends SMS notifications",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "netlify dev",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@netlify/functions": "^3.0.0",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
